# Phase 3: React Core API

## 목적

React 사용자 관점의 핵심 API를 안정화합니다.

## 작업 범주

- `NaverMapProvider`
- `NaverMap`
- `useNaverMap`
- map lifecycle(create/update/destroy)

## 완료 기준

- Provider/Map/Hook 조합으로 기본 시나리오 동작
- 생명주기 누락 없이 cleanup 보장
