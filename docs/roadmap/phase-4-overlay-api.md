# Phase 4: Overlay API

## 목적

지도 오버레이 관련 API를 컴포넌트 형태로 제공합니다.

## 작업 범주

- `Marker` wrapper
- children 기반 custom marker
- `InfoWindow` wrapper
- overlay cleanup

## 완료 기준

- 오버레이 생성/업데이트/제거가 누락 없이 동작
- React 생명주기와 overlay 생명주기 정합성 확보
